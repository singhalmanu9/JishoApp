import 'dart:convert';
import 'dart:io';
import 'dart:async';
main() {
  print("well, Dart is missing the EUC-JP encoder support still,"
      " so Python3 is the lang we'll use to actually generate the json");
}
